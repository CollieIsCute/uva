本題要求出任取幾位數，乘積總和，並取mod後最大者

解題方法為建立一表格，把輸入的數組陣列 a[] 從 a[1] (令陣列首個元素為 a[1])開始，依序加入。
每加入一個新的值，就更新表格。從任取一數直到任取N數，接著每加入一個數，
更新時就利用上一輪更新的值運算。
即：加入第n個數時，選取的組合可分為兩類
	1.有包含 a[n] 的取法
	2.未包含 a[n] 的取法
因此，加總上述兩項，即可得到以下結論：
若加入第n個數，則取m個的乘積總和即為之前已經有的 (加入a[n-1]取m-1個數的運算結果)*(a[n]) + (加入a[n-1]時取m個的運算結果)